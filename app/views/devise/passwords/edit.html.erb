<main class="container" data-turbo-cache='false'>
  <h2>Changer mon mot de passe</h2>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.hidden_field :reset_password_token %>

    <div class="field">
      <%= f.label :password, "Nouveau mot de passe" %><br />
      <%= f.password_field :password, autofocus: true, autocomplete: "new-password", placeholder:'6 charactères minimum' %>
      <% resource.errors.full_messages_for(:password).each do |message| %>
        <div class="text-red-400"><%= message %></div>
      <% end %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation, "Confirmer le mot de passe" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
      <% resource.errors.full_messages_for(:password_confirmation).each do |message| %>
        <div class="text-red-400"><%= message %></div>
      <% end %>
    </div>

    <div class="actions">
      <%= f.submit "Changer mon mot de passe" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</main>