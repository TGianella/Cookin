<div>
  <h1 class="font-bold text-4xl">Les sessions disponibles pour <%= @masterclass.title %></h1>
</div>

<% @meetings.each do |meeting| %>
  <div><%= link_to meeting.start_date, masterclass_meeting_path(@masterclass.title, meeting) %></div>
<% end %>
