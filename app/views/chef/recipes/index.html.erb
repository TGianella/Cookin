<div>
  <h1 class="font-bold text-4xl">Les recettes de <%= @chef.full_name %> </h1>
</div>

<% @recipes.each do |recipe| %>
  <div>
    <%= link_to "#{recipe.title}", chef_recipe_path(recipe.chef, recipe) %>
  </div>
<% end %>
