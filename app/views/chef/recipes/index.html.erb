<div>
  <h1 class="font-bold text-4xl">Les recettes de <%= "#{@chef.first_name} #{@chef.last_name}" %> </h1>
</div>

<% @recipes.each do |recipe| %>
  <div>
    <%= link_to "#{recipe.title}", recipe_path(recipe) %>
  </div>
<% end %>
