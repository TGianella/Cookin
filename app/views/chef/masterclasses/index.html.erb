<div>
  <h1 class="font-bold text-4xl">Les masterclasses de <%= @chef.full_name %> </h1>
</div>

<% @masterclasses.each do |masterclass| %>
  <div>
    <%= link_to "#{masterclass.title}", masterclass_path(masterclass) %>
  </div>
<% end %>
