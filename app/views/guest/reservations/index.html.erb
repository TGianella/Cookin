<div>
  <h1 class="font-bold text-4xl">Mes réservations</h1>
</div>
<% @reservations.each do |reservation| %>
  <%= render partial: "layouts/reservation", locals: { reservation: reservation } %>
<% end %>
